->Binary search is a searching algorithm that works on a sorted array, either in ascending or descending order.
It repeatedly divides the search interval in half to efficiently locate the target value.

